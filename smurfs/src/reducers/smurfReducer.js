const initialState = {
  isLoading: false,
  isAdding: false,
  isEditing: false,
  data: []
};

export const smurfReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
